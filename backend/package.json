{
  "name": "turbo-racers-starter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run --network localhost scripts/deploy.js",
    "mint": "hardhat run --network localhost scripts/mint-sample.js",
    "set-updater": "hardhat run --network localhost scripts/set-updater.js",
    "start": "node server.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "ethers": "^5.8.0",
    "express": "^4.18.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.17.0"
  }
}
